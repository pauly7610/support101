{
  "mcpServers": {
    "support101-agent-framework": {
      "command": "python",
      "args": ["-m", "packages.agent_framework.mcp_server"],
      "cwd": ".",
      "env": {
        "PINECONE_API_KEY": "${PINECONE_API_KEY}",
        "OPENAI_API_KEY": "${OPENAI_API_KEY}",
        "JWT_SECRET": "${JWT_SECRET}"
      }
    }
  }
}
