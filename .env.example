# ═══════════════════════════════════════════════════════════════════════
# Support101 — Environment Variables
# Copy to .env and fill in your values. Never commit real keys.
# ═══════════════════════════════════════════════════════════════════════

# ── Core ──────────────────────────────────────────────────────────────
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/support101
SECRET_KEY=your-secret-key-here
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002
REDIS_URL=redis://localhost:6379/0

# ── LLM Providers ────────────────────────────────────────────────────
# At least one is required. Multi-model support auto-discovers available providers.
OPENAI_API_KEY=your-openai-api-key-here
# ANTHROPIC_API_KEY=your-anthropic-api-key-here
# GOOGLE_API_KEY=your-google-api-key-here
# OLLAMA_BASE_URL=http://localhost:11434

# ── Pinecone (Vector Store) ──────────────────────────────────────────
PINECONE_API_KEY=your-pinecone-api-key-here
PINECONE_ENVIRONMENT=gcp-starter
PINECONE_INDEX_NAME=support-docs
PINECONE_CLOUD_PROVIDER=aws
PINECONE_REGION=us-west-2
# PINECONE_NAMESPACE=                    # Namespace for tenant isolation (default: "")
# PINECONE_RERANK_ENABLED=true           # Enable integrated reranking (default: true)
# PINECONE_RERANK_MODEL=bge-reranker-v2-m3  # Reranking model

# ── Voice I/O ────────────────────────────────────────────────────────
# Requires OPENAI_API_KEY for Whisper (STT) and TTS
VOICE_ENABLED=true
# VOICE_STT_MODEL=whisper-1              # Whisper model (default: whisper-1)
# VOICE_TTS_MODEL=tts-1                  # TTS model (default: tts-1)
# VOICE_TTS_VOICE=nova                   # Voice: alloy, echo, fable, onyx, nova, shimmer
# VOICE_TTS_SPEED=1.0                    # Playback speed 0.25-4.0
# VOICE_MAX_AUDIO_MB=25                  # Max upload size in MB

# ── LLM Cost Tracking ────────────────────────────────────────────────
# LLM_COST_TRACKING_ENABLED=true         # Enable/disable tracking (default: true)
# LLM_BUDGET_MONTHLY_USD=100.0           # Monthly budget in USD
# LLM_BUDGET_ALERT_THRESHOLD=0.8         # Alert at this % of budget (0.0-1.0)

# ── A2A Protocol ─────────────────────────────────────────────────────
# A2A_BASE_URL=http://localhost:8000     # Base URL for Agent Card discovery

# ── OpenTelemetry / Traceloop ────────────────────────────────────────
# TRACELOOP_API_KEY=your-traceloop-api-key
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_SERVICE_NAME=support101

# ── EvalAI Platform ──────────────────────────────────────────────────
# Workflow tracing, decision auditing, and governance
# EVALAI_API_KEY=your-evalai-api-key-here
# EVALAI_BASE_URL=https://ai-evaluation-platform.vercel.app
# EVALAI_ORGANIZATION_ID=123

# ── Agent Framework ──────────────────────────────────────────────────
# All optional. Agents gracefully degrade when env vars are not set.
# AGENT_FRAMEWORK_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/support101

# ── Continuous Learning ──────────────────────────────────────────────
ACTIVITY_GRAPH_ENABLED=true
ACTIVITY_GRAPH_NAME=support101
PLAYBOOK_MIN_SAMPLES=3
PLAYBOOK_MIN_SUCCESS_RATE=0.7

# ── Webhook Secrets (HMAC verification) ──────────────────────────────
# Leave empty to skip verification for that provider
# WEBHOOK_SECRET_ZENDESK=your-zendesk-webhook-secret
# WEBHOOK_SECRET_SLACK=your-slack-signing-secret
# WEBHOOK_SECRET_JIRA=your-jira-webhook-secret
# WEBHOOK_SECRET_GENERIC=your-generic-webhook-secret

# ── External Integrations (optional) ─────────────────────────────────
# EXTERNAL_TICKETING_URL=https://your-company.zendesk.com/api/v2
# EXTERNAL_TICKETING_API_KEY=your-ticketing-api-key
# EXTERNAL_CRM_URL=https://api.hubspot.com/crm/v3
# EXTERNAL_CRM_API_KEY=your-crm-api-key
# EXTERNAL_NOTIFICATION_URL=https://hooks.slack.com/services
# EXTERNAL_NOTIFICATION_API_KEY=your-notification-webhook-key
