# Example environment variables for Support101

# Backend Database
DATABASE_URL=postgresql://username:password@localhost:5432/support101

# Alembic (uses DATABASE_URL by default)
# Optionally override in alembic.ini or here
# ALEMBIC_DATABASE_URL=postgresql://username:password@localhost:5432/support101

# API Keys (replace with your own)
OPENAI_API_KEY=your-openai-api-key-here
PINECONE_API_KEY=your-pinecone-api-key-here

# Pinecone environment
PINECONE_ENVIRONMENT=gcp-starter

# Flask/Django/Other app secrets
SECRET_KEY=your-secret-key-here

# Redis (optional, for caching or async tasks)
REDIS_URL=redis://localhost:6379/0

# EvalAI Platform Integration (https://ai-evaluation-platform.vercel.app)
# Required for agent workflow tracing, decision auditing, and cost tracking
EVALAI_API_KEY=your-evalai-api-key-here
EVALAI_BASE_URL=https://ai-evaluation-platform.vercel.app
EVALAI_ORGANIZATION_ID=123

# ── Agent Framework Services ──────────────────────────────────────────
# These are OPTIONAL. Agents gracefully degrade to stub responses when
# env vars are not set. Add your own credentials to enable real integrations.

# Agent Framework Database (defaults to DATABASE_URL if not set)
# AGENT_FRAMEWORK_DATABASE_URL=postgresql+asyncpg://username:password@localhost:5432/support101

# Pinecone Vector Store (uses PINECONE_API_KEY above)
PINECONE_INDEX_NAME=support-docs
# PINECONE_CLOUD_PROVIDER=aws
# PINECONE_REGION=us-west-2

# External Ticketing System (Zendesk, Jira, ServiceNow, etc.)
# EXTERNAL_TICKETING_URL=https://your-company.zendesk.com/api/v2
# EXTERNAL_TICKETING_API_KEY=your-ticketing-api-key

# External CRM (Salesforce, HubSpot, etc.)
# EXTERNAL_CRM_URL=https://api.hubspot.com/crm/v3
# EXTERNAL_CRM_API_KEY=your-crm-api-key

# External Notification Service (Slack, PagerDuty, etc.)
# EXTERNAL_NOTIFICATION_URL=https://hooks.slack.com/services
# EXTERNAL_NOTIFICATION_API_KEY=your-notification-webhook-key

# ── Continuous Learning System ─────────────────────────────────────────
# Activity Graph (Apache AGE on Postgres — optional, falls back to in-memory)
# Requires: CREATE EXTENSION age; in your Postgres instance
ACTIVITY_GRAPH_ENABLED=true
ACTIVITY_GRAPH_NAME=support101

# Playbook Engine
PLAYBOOK_MIN_SAMPLES=3
PLAYBOOK_MIN_SUCCESS_RATE=0.7

# Webhook Secrets (HMAC verification — leave empty to skip verification)
# WEBHOOK_SECRET_ZENDESK=your-zendesk-webhook-secret
# WEBHOOK_SECRET_SLACK=your-slack-signing-secret
# WEBHOOK_SECRET_JIRA=your-jira-webhook-secret
# WEBHOOK_SECRET_GENERIC=your-generic-webhook-secret

# Add any other required variables below
